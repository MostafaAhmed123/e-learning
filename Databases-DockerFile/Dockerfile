# FROM mysql:8.0

# ENV MYSQL_ROOT_PASSWORD=mostafa
# ENV DATABASE1=users
# ENV DATABASE2=courses
# ENV DATABASE3=e-learning
# EXPOSE 3306
# RUN mysql -u root -p"${MYSQL_ROOT_PASSWORD}" -e "CREATE DATABASE IF NOT EXISTS ${DATABASE1};"
# RUN mysql -u root -p"${MYSQL_ROOT_PASSWORD}" -e "CREATE DATABASE IF NOT EXISTS ${DATABASE2};"
# RUN mysql -u root -p"${MYSQL_ROOT_PASSWORD}" -e "CREATE DATABASE IF NOT EXISTS ${DATABASE3};"

FROM mysql:8.0

ENV MYSQL_ROOT_PASSWORD=mostafa
ENV MYSQL_DATABASE=users

EXPOSE 3306
